spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/jpa_exam
    username: root
    password: green502
  jpa:
    hibernate:
      ddl-auto: update #create는 변경이 있을 때, create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true